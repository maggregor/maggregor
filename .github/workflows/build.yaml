name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/workflows/composite-action-setup

      - name: Unit Tests
        run: |
          pnpm test:unit

      - name: Integration Tests
        run: |
          pnpm test:integration

      - name: E2E Tests
        run: |
          pnpm test:e2e
