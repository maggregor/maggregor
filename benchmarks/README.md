# Benchmarks with BenchmarkJS

## Get started 

### Run all benchmarks
    
```bash 
pnpm benchmark
```

### Run a specific benchmark

```bash
pnpm benchmark --name=[benchmark-name]
```

### Enable output of benchmark results
**Output directory:** `benchmarks/.results`
```bash
pnpm benchmark --output
```
